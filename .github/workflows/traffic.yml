name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Traffic Stats
        # 👇 改回 main，因為 master 不存在
        uses: jgehrcke/github-repo-stats@main
        with:
          ghtoken: ${{ secrets.GH_TOKEN }}
          # 👇 關鍵救星：指定它把檔案存到根目錄，不要去存那個不存在的 ghrs-data 資料夾
          stargazer-ts-snapshots-inoutpath: stargazer-snapshots.csv
