name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Traffic Stats
        uses: jgehrcke/github-repo-stats@master
        with:
          ghtoken: ${{ secrets.GH_TOKEN }}
          # 👇 這是關鍵修改：我們指定它把檔案存在根目錄，避開 "ghrs-data" 資料夾報錯的問題
          stargazer_ts_snapshots_inoutpath: stargazer-snapshots.csv
