name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # 👇 這是修復 Bug 的關鍵步驟：我們先幫機器人建好它找不到的資料夾
      - name: Fix Bug - Create Data Folder
        run: mkdir -p ghrs-data

      - name: Run Traffic Stats
        uses: jgehrcke/github-repo-stats@master
        with:
          ghtoken: ${{ secrets.GH_TOKEN }}
