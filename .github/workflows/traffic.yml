name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  traffic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Traffic to Badge
        uses: yi-jiayu/github-traffic-to-badge@v1.4.0
        with:
          # 👇 這個工具的參數名稱叫 accessToken
          accessToken: ${{ secrets.GH_TOKEN }}
          # 指定數據要存在 traffic 分支
          branch: traffic
