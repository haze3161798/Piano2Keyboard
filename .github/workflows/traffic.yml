name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  traffic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Repository Traffic
        # ✅ 改用 master 版本，避開之前的語法錯誤
        uses: sangonzal/repository-traffic-action@master
        with:
          github_token: ${{ secrets.GH_TOKEN }}
