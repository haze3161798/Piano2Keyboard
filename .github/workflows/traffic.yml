name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  traffic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Repository Traffic
        uses: sangonzal/repository-traffic-action@master
        env:
          # 👇 這是關鍵！這個工具不收 'with'，只收 'env'，而且名字一定要是這個
          TRAFFIC_ACTION_TOKEN: ${{ secrets.GH_TOKEN }}
