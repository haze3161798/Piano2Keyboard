name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      # 👇 這是關鍵！我們先幫機器人建好它找不到的資料夾，這樣就不會報錯了
      - name: Create Data Folder (Fix Bug)
        run: mkdir -p ghrs-data

      - name: Run Traffic Stats
        uses: jgehrcke/github-repo-stats@main
        with:
          ghtoken: ${{ secrets.GH_TOKEN }}
