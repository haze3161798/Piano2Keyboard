name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Run Traffic Stats
        uses: jgehrcke/github-repo-stats@main
        env:
          # 👇 這是密技！我們直接用環境變數覆蓋那個會報錯的路徑設定
          # 強迫它把星星數據存在根目錄，而不是那個不存在的資料夾
          INPUT_STARGAZER_TS_SNAPSHOTS_INOUTPATH: stargazer-snapshots.csv
        with:
          ghtoken: ${{ secrets.GH_TOKEN }}
