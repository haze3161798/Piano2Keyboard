name: Traffic Stats

on:
  schedule:
    - cron: "0 23 * * *"
  workflow_dispatch:

jobs:
  traffic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Repository Traffic
        uses: sangonzal/repository-traffic-action@master
        env:
          # 👇 關鍵修改：要把 'with' 改成 'env'，而且變數名稱必須是這個
          TRAFFIC_ACTION_TOKEN: ${{ secrets.GH_TOKEN }}
